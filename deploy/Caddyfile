{$APP_DOMAIN} {
    encode zstd gzip
    reverse_proxy app:8000 {
        flush_interval -1
    }
    header {
        Strict-Transport-Security "max-age=31536000; includeSubDomains"
        -Server
    }
}
